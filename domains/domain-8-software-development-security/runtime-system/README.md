# Runtime Systems

Writing software is only half the story—running it is the other half.

Runtime systems provide the environment where applications actually execute. This includes operating systems, application servers, virtual machines, and runtime libraries.

Understanding runtime environments is important because many vulnerabilities occur during execution, not just during development.